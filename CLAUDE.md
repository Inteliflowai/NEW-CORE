# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## ⚠️ Next.js 16 — verify before you code

This project runs **Next.js 16.2.9 with React 19** (see `AGENTS.md`). This is newer than most training data: APIs, conventions, and file structure may differ from what you remember (e.g. async `params`/`searchParams`, `cookies()`/`headers()` are async, caching defaults changed). The full version-matched docs are bundled locally — read the relevant guide before writing framework code:

```
node_modules/next/dist/docs/01-app/   # App Router (this project uses it)
```

Do not assume an API exists; confirm it against the bundled docs.

## Commands

```bash
npm run dev      # start dev server (Turbopack) at http://localhost:3000
npm run build    # production build (Turbopack) — also type-checks the whole project
npm run start    # serve the production build (run after `npm run build`)
npm run lint     # ESLint (flat config: next/core-web-vitals + next/typescript)
```

- **Type-checking:** there is no standalone `typecheck` script; `npm run build` runs the TypeScript pass. For a faster check without emitting, use `npx tsc --noEmit`.
- **Tests:** Vitest 4.x (+ `@testing-library/react`, `jsdom`). `npm test` (run-once) · `npm run test:watch` · single file: `npx vitest run <path>`. Default env is `node`; **React component tests must start with `// @vitest-environment jsdom` then `import '@/test/setup-dom';`** (the established pattern — follow each existing file's header). `npm run prebuild` runs the **un-bypassable WCAG-AA contrast gate** (`npm run a11y` → `scripts/a11y/contrast-check.ts`).

## Architecture

Standard Next.js App Router scaffold; the structure is conventional, so only the non-obvious points are noted:

- **App Router under `src/`.** Routes live in `src/app/`. `layout.tsx` is the root layout (wraps every page); `page.tsx` is a route's UI. New routes are folders under `src/app/` containing a `page.tsx`; API routes are `route.ts` files. Server Components are the default — add `"use client"` only when a component needs browser APIs or state.
- **Import alias:** `@/*` maps to `src/*` (`tsconfig.json`). Prefer `@/...` over long relative paths.
- **Styling:** Tailwind CSS **v4**, configured via `@tailwindcss/postcss` in `postcss.config.mjs` — there is no `tailwind.config.js`. Global styles and Tailwind layers are in `src/app/globals.css`.
- **Design tokens (single source of truth):** the tokens — Tier-1 ramps, Tier-2 semantic slots, Tier-3 role×intensity bindings, the `@theme` exposure, and the **motion** tokens (for framer-motion) — live in **`src/lib/design/tokens.ts`**. The CSS in `globals.css` between the `TOKENS:GENERATED:BEGIN/END` markers is **generated** from it by `npm run tokens:gen`. **Never hand-edit the generated region** — edit `tokens.ts` and regenerate; `npm run tokens:check` (run in `prebuild`) fails on drift. Components keep the same token classes (`text-fg`, `bg-brand`, `shadow-sticker`…); never hardcode hex/spacing/type, and **ask before inventing a token**.
- **`next.config.ts`** is currently empty; add framework config there (TypeScript, not JSON).
- **`AGENTS.md`** holds the cross-tool agent rules and is the source of the Next.js-version warning above.

## CORE v2 — status, disciplines & roadmap (read before building features)

**Product framing.** V2 is a **Beta, not an MVP** — a re-build of the proven V1 product (`C:/users/inteliflow/core`, the UX/feature reference) to contract scope. Pilots are the feedback engine before a fully-tested GA. Aim for **V1-parity** on the pilot-relevant surface; don't trim to a bare MVP, don't gold-plate beyond V1.

**Decision trail is in memory.** `MEMORY.md` (auto-loaded) indexes the project decisions — **read it first.** Designs live under `docs/superpowers/specs/`, plans under `docs/superpowers/plans/` (each plan has a `grounding/` folder of verbatim current-code facts + a review log).

**Built, merged & DEPLOYED LIVE** (`main`; production at **newcore.inteliflowai.com**). Snapshot as of 2026-06-20:
- **Foundation (Plan 4b):** demo seed (`scripts/seedDemo.ts`/`resetDemo.ts`, `src/lib/demo/`), `provisionTrial` engine + trial libs (`src/lib/trial/`), `GET /api/teacher/classes` + `STAFF_ROLES`, shared component kit (`src/components/core/`), copy helpers (`src/lib/copy/`), super-admin `POST /api/admin/provision-trial` + `(super-admin)/provision`.
- **Auth-entry UI (merged, `dd44648`):** universal `/login` (slide-image design), `/set-password`, `/logout`, `/trial-expired`, `auth/callback`, proxy gate + 5 per-role layout guards. Every surface is now reachable.
- **Teacher pop-art app shell + screens (built + deployed, `2de43ef`):** `(teacher)/_components/TeacherShell` = cobalt rail + lime active sticker + real CORE logo on white plate; `TeacherSidebar`/`SidebarNav`/`TeacherTopbar`; the teacher screens exist (Today, Roster, Spark Challenges, Gradebook, Alerts, High-fives, Insights, Library, Upload, student drill-in).
- **SPARK integration — Phase 1 + Phase 2, ALL merged + deployed live** (V2 `main` `64e92b9`, SPARK repo `master` `15c7085`): teacher half (create-notify, completion ingestion, `/challenges`), student launch (`(student)/student/assignments` + `POST /api/attempts/spark-launch`, hand-rolled HS256 JWT), super-admin `(super-admin)/schools` + `POST /api/admin/spark-enable` (one-click BOTH-sided provisioning), SPARK-repo `POST /api/integration/provision-school`. Routes verified live. **Each CORE school maps 1:1 to its own SPARK school** (`resolveCoreBaseUrl` uses `.maybeSingle()`). V2 demo→dedicated SPARK school `c4d2d2db`; V1 untouched.
- **Standing recommendation:** ROTATE `CORE_SPARK_API_SECRET` (the literal lived in private-repo git history); now also affects the launch JWT.

**Binding disciplines (non-negotiable on every surface):**
- **Coach posture (the moat):** every surface obeys [`COACH-POSTURE.md`](COACH-POSTURE.md) — speaks first (observation, not a metric), one thing at a time, plain human language (no geek/raw stats), notices→suggests→confirms (never decides), quiet on good days, **not a chatbot**. User-facing strings go to `STRINGS-FOR-BARB.md` as drafts; Barb gates all copy.
- **Four-audience discipline:** student/parent surfaces never show the mastery-band enum or a raw risk number (band label only); CL verbs / diagnoses / divergence / misconceptions are **teacher-only**; growth is "you vs your own past" (never peer-relative, never fabricated — cold-start instead); observational, not diagnostic. Enforced at the string boundary in `src/lib/copy/` (pure helpers + `leakGuard`).
- **"Assignments", never "Homework"** in any UI/copy (legacy term survives only in DB identifiers like `homework_attempts`).
- **WCAG-AA + deep-ink:** no hardcoded hex / arbitrary `[var(--..)]` in components — Tier-2 token classes only; content text is deep-ink (`text-fg`), not `text-fg-muted`.
- **Auth chain (every protected route):** `await createServerSupabaseClient()` → `auth.getUser()` → STAFF_ROLES gate → object-level IDOR guard (`src/lib/auth/guards.ts`) → `createAdminSupabaseClient()` (synchronous; reads `SUPABASE_SECRET_KEY`; **bypasses RLS — RLS is NOT the IDOR backstop**).

**Dev workflow.** Brainstorm → spec → `writing-plans` → **subagent-driven-development** (fresh implementer per task + task review + final whole-branch review). Ledger at `.git/sdd/progress.md`. **The in-house adversarial Workflow is the primary review** — Codex has been unreliable/hung; don't block on it.

**DONE + MERGED TO PROD (`main` `532d36e`, 2026-06-20) — Pop-Art polish pass.** A long screenshot-driven (Playwright) + adversarial-reviewed cosmetic/UX pass shipped: full-bleed gallery **login** (SPARK pop-art slides, real CORE+SPARK logos, frosted-cobalt translucent sign-in card in a left column, on-glass text deep-inked for AA over the art), **teacher** Pop-Art content (sticker-label card titles, tone-tinted **color-coded** cards via a new `Card` `tone` prop + `SectionLabel`, denser/smaller text, `pop-dots`/`pop-canvas` textures), **student** whole-child redesign (tone-tinted Skill Map rows), the **class-picker flash fix** (`SidebarNav` carries `?class=` + `firstClassIdForTeacher` server-default redirect), the SPARK sticker tilt, and the **/assignments 404 fix** (deferred the dead `Open Assignments` CTA through IdentityHeader/PriorityRecommendation/WholeChildRail). Design tokens: 3-tier (`src/app/globals.css`), teacher=cobalt, `sidebar-active`=lime, `--shadow-sticker`/`--shadow-sticker-lg`, `@utility pop-dots`/`pop-canvas`. Deferred Nit: the 5 login slide source PNGs are ~23 MB (next/image optimizes delivery; compress sources later).

**NOW — the "meat and potatoes" V1-parity feature build (see [[v2-parity-program-meat-and-potatoes]] memory).** Data-producers first, epics **1→4**. **Status as of 2026-06-23 (epics 1–4 below; Content Studio segments are tracked in the DONE paragraphs that follow):**
1. **Student Quiz Runner — ✅ DONE + merged + deployed live** (`main`; turns on the behavioral-signals pipeline).
2. **Non-SPARK Assignment Player — ✅ DONE + merged + deployed live + smoke-tested.** Segments 1–2 (core player: read→typed tasks→autosave→AI grade the student SEES) + **Segment 3 Teli tutor** (Socratic, bounded 4-rung hint ladder, **FAIL-CLOSED never-reveals-the-answer** guard, real `teli_hint_count`/`effort_label` into the moat; `claude-opus-4-8`; route `POST /api/attempts/homework-tutor`; migration 0016). GOTCHA banked: newest Claude reasoning models (opus-4.x/fable) reject the `temperature` param (400) — `claudeChat` now omits it for them. The player's drawing canvas and voice were folded into the Content Studio epic and are now DONE + LIVE (drawing → Content Studio Seg 5 `61ea954`; voice → Content Studio Seg 3 `71b66ef`; see the Content Studio DONE paragraphs below).
3. **Teacher completion (Epic 3) — HYBRID: 3a full gradebook, 3b lean trio (see [[v2-epic3-teacher-screens]]).** **3a — FULL GRADEBOOK ✅ DONE + MERGED + DEPLOYED LIVE** (`main` `f1f5494`, newcore.inteliflowai.com, 2026-06-22; NO migration): assignment-column grid (7 cell statuses, class-avg over assignments only) + separated read-only "Diagnostic checks — not graded" quiz section + click-cell drill-in (grade override → existing `teacher_score`; reteach → `allow_redo`). Files: `src/lib/gradebook/loadGradebook.ts`, `POST /api/teacher/gradebook/override` (override-wins: writes ONLY teacher_score/teacher_notes/allow_redo), `GradebookGrid`/`DiagnosticChecksSection`/`GradebookDrillIn` + `(teacher)/gradebook/page.tsx`, `src/lib/copy/effortLabelPhrase.ts`. Reviewed via committee + `/code-review` + `/frontend-design-audit` + **codex** (codex caught 2 genuine P2 the in-house missed); all fixed; gates vitest 1899/1899, tsc 0, build 0, a11y 49/49. Deferred cosmetic polish + Barb copy gating are in the ledger / `STRINGS-FOR-BARB.md §Gradebook`. **3b — LEAN Alerts + High-Fives + Insights ✅ DONE + MERGED + DEPLOYED LIVE** (`main` `44c976c`, 2026-06-22; migration **0017** `alerts`+`high_fives` WITH RLS, applied live): alerts **reconcile-on-read** (triggers `low_quiz`/`low_assignment`/`reteach_flag`/`reteach_review`/`strong_result`; auto-clear + manual "Mark handled"; divergence DROPPED as a trigger), AI-drafted guardrailed high-fives (`validateHighFive`) + tiny student note view, quiet Insights hub (band mix + observation + skill gaps, no charts). (the deferred player drawing-canvas + voice were later delivered inside Content Studio — Seg 5 and Seg 3 respectively, both LIVE; see below.)
4. **Parent dashboard + AI narrative** (9-paragraph summary over already-ported leak-guard primitives) — after Epic 3.

**DONE + MERGED TO PROD — Design sprint: token SoT → signature-moment → coach-motion retrofit (2026-06-22).** (1) **Design-token TS SoT refactor** (`fad3c16`): all tokens (color + spacing + type + **motion**) now live in **`src/lib/design/tokens.ts`**; the `globals.css` token region is **GENERATED** via `npm run tokens:gen` (never hand-edit; `tokens:check` in prebuild fails on drift). (2) **Signature-moment feel prototype** (`661b799`) + **[`FEEL-DIRECTION.md`](FEEL-DIRECTION.md)** — the motion/personality SoT (sibling to `COACH-POSTURE.md`): the coach four-beat **NOTICE→SPEAK→INVITE→DEFER** in framer-motion, 3 emotional registers. (3) **Coach-motion retrofit** (`main` `361e5ee`, **LIVE**): brought the four-beat to the 4 real coach surfaces — Teli, the "Worth a look?" coach card, the Alerts feed, and the High-Five note (teacher composer coach-mark + DEFER calm, **and** the student receives it with a celebratory spark) — via shared, token-sourced **`src/lib/design/coachMotion.ts`** builders; **`prefers-reduced-motion` snaps every beat** (WCAG-AA). `framer-motion ^12.40` added; NO migration. Whole-branch adversarial review (5 lenses) all ready-to-merge; Playwright-previewed live. **Binding (Marvin):** UI work uses frontend-design + frontend-design-audit but proposed visual mods are **propose-only — approve before applying**; preview screens via Playwright ([[v2-frontend-review-workflow]]). Never hardcode hex/spacing/type/**motion** — ask before inventing a token ([[v2-design-token-discipline]]).

**DONE + MERGED TO PROD — Gradebook v1.1: dates + trend (`main` `78572a9`, newcore.inteliflowai.com, 2026-06-23; migration 0018 applied live + verified + demo reseeded; smoke-tested in prod).** Per-day assignment columns: `loadGradebook` colKey is now `lesson:<lesson_id>:<assigned-day>` (was `lesson_id` only — collapsed all days), so same-lesson daily work splits into separate **dated** columns (different-lesson-same-day stay separate too — better than V1). New **`assigned_at`** timestamp (migration 0018, stamped at generation + seed; the third first-class date alongside due + submitted). Columns ordered chronological, windowed to the most-recent **60** (`MAX_ASSIGNMENT_COLS`, non-silent `console.warn` on truncation), grid shows recent ~12 with a **"Show earlier"** expand. Lesson titles fetched for column headers (header subline `Assigned … · Due …`). **Per-student grade trend** (`src/lib/gradebook/loadStudentGradeTrend.ts` → graded `homework_attempts` by `graded_at`, override-wins, climbing/steady/sliding) rendered by a shared **`src/components/core/GradeTrendSparkline.tsx`** (token SVG, `--brand` stroke for WCAG-AA, cold-start <2 pts) in **both** the gradebook drill-in (via `GET /api/teacher/gradebook/trend`, STAFF_ROLES-gated) **and** the One-Student profile ("Grades over time", beside "Growing"). Barb's **cell hover tooltip** = white pop-art card, all three dates (Assigned · Due · Turned in, on-time/late) + Escape-dismiss + dates folded into the cell aria-label (SR path). Built via subagent-driven 8-task TDD (4 dependency waves) + 5-lens whole-branch review (col-cap + sparkline-contrast + tooltip-a11y + route-role fixes applied; one reviewer false-positive rejected). Gates: tsc 0, vitest 2045/2045, build 0 (a11y + tokens). Also fixed a **pre-existing** `resetDemo.ts` cascade bug (nulls `quiz_questions.skill_id` before the school delete). Deferred Minors logged. **Known PRE-EXISTING bug (not this epic):** React #418 hydration mismatch on every teacher page (the time-based "Good morning" greeting in the topbar) — needs a client-only/`suppressHydrationWarning` fix.

**DONE + MERGED TO PROD — Content Studio Segment 1 (Teacher Authoring program; `main` merge `2881acb`, newcore.inteliflowai.com, 2026-06-23; migration 0019 live + verified; prod deploy READY).** The first slice of the **Authoring Platform program** ([[v2-authoring-platform-program]]): automatic **upload → parse → quiz** (`UploadStudio`; `POST /api/teacher/lessons/{upload,parse}`; unpdf+mammoth extraction; `lesson-uploads` private Storage bucket — NEW CORE's first bucket), **Jaccard fuzzy dedup + sha256 exact-dup 409** (`src/lib/lessons/duplicateDetect.ts`), flat **Lesson + Quiz libraries**, and quiz **edit/publish/archive** (`POST /api/teacher/quizzes/manage`; `status='published'`+`published_at` = the student-visibility gate). **Plus a live-feedback refinement (Marvin, 2026-06-23):** (a) read-only **lesson-plan viewer** — "View lesson" → `LessonViewPanel` shows `parsed_content` (summary/objectives/key ideas/vocabulary/likely-misconceptions); (b) **Subject · Grade categorization** of BOTH libraries (section headers + Subject/Grade dropdown filters via shared `src/lib/content/category.ts`) + a **Class selector** (`ClassSelect`: "one class at a time" — IS the sidebar active class, navigates `?class=`, shown only when teacher has >1 class; no cross-class view). Migration 0019 adds `lessons.file_hash`/`source`. Built TDD + 5-lens adversarial review (verified findings; 4 Important + 5 Minor fixed — incl. a real filter trim-parity bug). Gates: tsc 0, **vitest 2144/2144**, build 0 (tokens in sync, WCAG-AA). Strings → `STRINGS-FOR-BARB.md §Content Studio`. Deferred Minors logged (app-wide `text-[10px]`/`tracking-[…]` arbitrary values; modal-inert house pattern). **Security advisories flagged to fix later** ([[security-advisories-deferred]]): V1/Core-platform has serious LIVE ERRORs (auth.users exposed to anon; RLS-off tables) — NOT V2; V2 NEW CORE has its own all-WARN set.

**DONE + MERGED TO PROD — Content Studio Segment 2: AI Lesson Generator + URL import (`main` merge `711de82`, newcore.inteliflowai.com, 2026-06-23; migration 0020 applied live + verified; Vercel deploy READY).** The Upload page becomes a **3-tab Content Studio** — *Upload a file* (Seg 1) · *From a URL* · *Generate with AI* (`ContentStudioTabs`, ARIA tablist). **Engine (import-safe):** `generateLesson` + `segmentUnit` (`src/lib/engine/lessonGenerate.ts`; reuse `ParsedLessonSchema` + `proposed_standards`; `OPENAI_GEN_MODEL`/json_object/throw `LlmExhaustedError`) and `extractTextFromUrl` (`parseUrl.ts`; **SSRF-guarded** — manual redirect loop re-validating every hop + numeric-IP/IPv4-mapped canonicalization). **Routes:** `POST /api/teacher/lessons/generate` (single OR multi-day unit; per-day generation off each segment's focus; `day_index=i+1`; standards state resolved from the **class** → `schools.state`), `POST /api/teacher/lessons/import-url` (public/link-shared URLs incl. shared Google Docs → existing `parseLesson`), and the `lessons/manage` **`edit`** action (validates `parsed_content` via `GeneratedLessonSchema`). **Standards:** AI proposes US-state-framework codes, teacher confirms (**default-checked** opt-out) → persisted to `lessons.standard_codes`/`standard_framework` (short label), surfaced read-only in `LessonViewPanel`/library. **Review surface** `LessonReviewEditor` (editable passage/goals/vocab/misconceptions as newline textareas, multi-day **Day pager**, "Save & make quiz" / "Save unit & make quizzes" → per-day `quizzes/generate`). Migration **0020** adds `lessons.chapter_title`/`day_index`/`standard_codes`/`standard_framework` + `schools.state`. Built subagent-driven TDD (13 tasks, 5 waves) + a **5-lens whole-branch adversarial Workflow review** (32 confirmed/plausible, 1 refuted) → 2 fix-waves → Opus re-verify: caught + fixed a **CRITICAL SSRF redirect-bypass**, a **CRITICAL URL-tab 503 object-render crash** (`readErrorMessage`), **standards-saving-empty**, **multi-day-duplicate-days**, a DB-error-text leak, and unvalidated `parsed_content`. Gates: tsc 0, **vitest 2219/2219**, build 0 (a11y + tokens). Playwright-previewed (3 tabs + form live; review editor via throwaway page) + Marvin-approved before merge. Strings → `STRINGS-FOR-BARB.md §Content Studio — Seg 2`. **Deferred-minor (logged):** exotic hex-grouped IPv4-mapped-IPv6 SSRF literal (all realistic vectors blocked), modal focus-restore-to-body, numDays clamp announce, URL-imports-standards-less (by design); **`schools.state` column added but UNPOPULATED** (teacher confirms state inline; auto-populate at provisioning → Google epic).

**DONE + MERGED TO PROD — Content Studio Segment 5: student drawing canvas + teacher review-of-submitted-work (`main` merge `61ea954`, newcore.inteliflowai.com, 2026-06-23; migration 0021 applied live + verified; Vercel deploy READY).** Students answer a task by **drawing** (lean native-canvas `DrawingCanvas` — pen/eraser, colors, widths, undo/clear, mouse+touch → PNG) or attaching a **photo**, written into the EXISTING `responses.tasks[step].image_url` slot (NO contract reshape; autosave/submit/grader already consume it; a drawing-only answer counts as complete; `canvasUsed` moat signal flipped). Drawings live in a **private `student-drawings` bucket** (migration 0021) served by an **auth'd image proxy** `POST/GET /api/attempts/drawing` — the persisted `image_url` is a stable proxy link (`/api/attempts/drawing?path=…`), NEVER a public/expiring URL; the GET authorizes student-owns OR staff+`guardStudentAccess`. Teacher **"Student's work"** panel in the gradebook drill-in (`GET /api/teacher/gradebook/attempt`, on-demand so the gradebook stays light) shows per-task text + drawing thumbnails (click-to-enlarge) + the AI's feedback. Built subagent-driven TDD (8 tasks) + a **5-lens whole-branch adversarial review** (27 confirmed/plausible, 2 refuted) → 3 fix-waves → re-verify: caught + fixed a **CRITICAL unvalidated-`image_url` exfil** (a student could store an arbitrary external URL the teacher's authenticated browser would then fetch on the panel — now validated as an OWNED proxy link at `homework-draft` + `homework-submit` AND render-guarded via `src/lib/assignments/imageUrlGuard.ts`), an **IMPORTANT drawing data-loss** (attach-then-navigate within the 3s autosave debounce dropped it — now persists immediately), plus several a11y (Escape closing the whole drill-in; raw-hex/number canvas labels; "drawing" labels on photos). Gates: tsc 0, **vitest 2267/2267**, build 0 (a11y + tokens). Playwright-previewed (canvas + teacher panel) + Marvin-approved. Strings → `STRINGS-FOR-BARB.md §Content Studio — Seg 5`. **Deferred-minor (logged):** orphaned drawing blobs not deleted on replace (cleanup sweep follow-up); eraser is white-paint-over (fine on the opaque canvas); bucket-level size/MIME limits omitted (app-layer is the gate, mirrors 0019); `ai_feedback` prose unguarded on the teacher-only panel.

**DONE + MERGED TO PROD — Content Studio Segment 3: voice — dictation + read-aloud (`main` merge `71b66ef`, newcore.inteliflowai.com, 2026-06-23; NO migration; Vercel prod deploy promoted on push).** This **completes the Content Studio epic.** Two voice capabilities, both stateless (no DB, no Storage): (1) **Dictation** — a `MicButton` records (MediaRecorder) → `POST /api/attempts/transcribe` (OpenAI **whisper-1**, `resilientAudioTranscription`) → text, mounted on BOTH the teacher "describe a lesson" box (`GenerateLessonStudio`) AND the student answer box (`TaskCard`); dictated text **appends, never replaces**. (2) **Read-aloud** — a `ReadAloudButton` calls `POST /api/attempts/tts` (OpenAI **tts-1**, voice `nova`, `resilientTextToSpeech`) → streamed `audio/mpeg`, on the assignment reading passage (prefers the conversational `content.audio_script`, falls back to the written passage), feeding the **`ttsPlayCount`** behavioral signal (exploratory style). Both routes are `getUser`-gated only (correct for a stateless, owns-its-own-bytes design — no IDOR surface), `runtime='nodejs'` + `maxDuration=60`. Built subagent-driven TDD (9 tasks) + a **whole-branch adversarial review** (42 agents, 34 confirmed/plausible, 3 refuted) → one fix-wave (`c8fae20`) → re-verify: closed the confirmed **mic-stays-hot-on-unmount** + **audio-keeps-playing-on-unmount** lifecycle leaks (privacy/memory), the click-during-getUserMedia stuck-recording race, a11y (working/loading states in the accessible name, deep-ink error text, visible read-aloud error, WCAG 2.5.3 label), the empty-content-type 415 bypass, the dangling AbortController retry timer, and a raw-error-message info leak in the shared `errorEnvelope`. Gates: tsc 0, **vitest 2300/2300** (+10 lifecycle/error regression tests), build 0 (a11y + tokens). Strings → `STRINGS-FOR-BARB.md §Content Studio`. **Deferred (Marvin's call, 2026-06-23): per-user RATE LIMITER on the two voice routes** — any authenticated user (incl. a student) can call them in a loop; V1 had usage caps, V2 has none and no rate-limit infra anywhere in `src/`; `maxDuration`+MIME bound the worst case but there is NO calls-per-minute backstop → small follow-up. Also deferred-minor: TTS >4096 chars silently truncated (audio_scripts are short); 1KB "too short" floor is a weak proxy.

**DONE + MERGED TO PROD (CODE) — Google Classroom epic, Segment 1: Connect + Token Vault (`main` merge `5b6f04e`, 2026-06-24; migration 0022 APPLIED + verified live to NEW CORE — see below).** The spine of the GC epic (5-segment epic per [[v2-authoring-platform-program]]; spec `docs/superpowers/specs/2026-06-23-google-classroom-design.md`, grounding `docs/.../grounding/2026-06-23-google-classroom/`). **Per-teacher Google OAuth connect** with an **AES-256-GCM-encrypted token vault** (Marvin: encrypt-at-rest, overriding V1's plaintext). Lib `src/lib/google/{config,crypto,oauthUrls,tokens,profile}.ts` (scopes + token-at-rest crypto via `node:crypto` zero-dep + authorize-URL builder + code→token exchange + lazy-refresh token-manager + profile). Routes (all **teacher-only**): `GET /api/teacher/google/connect` (random-uuid CSRF state cookie → Google consent), `GET /api/auth/google/callback` (verify state → exchange → **reject unverified email** → store ENCRYPTED connection; never creates a session), `GET /api/teacher/google/scope-check` (tokeninfo diff vs `GC_REQUIRED_SCOPES` + **stored-granted_scopes fallback** so a tokeninfo outage doesn't false-storm reconnect), `POST /api/teacher/google/disconnect` (deletes own row; UI won't show "disconnected" on a failed delete). Teacher connect/reconnect/disconnect card at **`/settings/google`** + a sidebar entry (shown to ALL teachers — Marvin chose pilot-wide; the per-school `schools.google_classroom_enabled` gate is **NOT wired**, a deferred follow-up). Migration **0022** `google_connections` (RLS deny-by-default, admin-client only; encrypted token columns, no plaintext). Built subagent-driven TDD (12 tasks, per-task review each) + a **PRE-CODE plan review (22 findings folded in before any code)** + a **whole-branch adversarial review (17 findings, 0 refuted → fix-wave `855508c`, re-reviewed clean):** unverified-email guard, disconnect-failure UI, scope-check empty-scope fallback, CSRF-before-denied reorder, secure-flag dev-conditional, + 5 test-coverage gaps. Gates: tsc 0, **vitest 2351/2351**, build 0. **✅ NOW LIVE — go-live COMPLETE 2026-06-24.** Reused V1's OAuth client (`636434037411-…`) + added the V2 redirect URI `…/api/auth/google/callback` in GCP; `GOOGLE_CLIENT_ID/SECRET/REDIRECT_URI` + **`GOOGLE_TOKEN_ENC_KEY`** all set in Vercel; migration 0022 APPLIED + verified live to NEW CORE (`pmdzxwppdlnddtnkoarc`; RLS on, encrypted-only, advisors all-WARN). A teacher completed a real Google connect — encrypted access+refresh tokens + 9 scopes verified in `google_connections` (the reused `client_secret`↔`client_id` pair is valid). Strings → `STRINGS-FOR-BARB.md §Google Classroom — Seg 1`. **Deferred:** per-school flag gate; `external_identities` email/last_seen_at + resolve helper → Seg 2 (migration 0023); `schools.state` populate (open item); Seg-4 proxy carve-out for the shared callback URI; typed reauth error; cosmetic nits (inert `justConnected` prop, stale import).

**DONE + MERGED TO PROD — Google Classroom epic, Segment 2: Roster import + two-way sync + nightly cron (`main` ← branch `639b014`, 2026-06-24; migration 0024 APPLIED + verified live to NEW CORE).** Pull a teacher's GC roster into CORE: paginated `GET /api/teacher/google/{courses,roster}` (teacher-only; reconnect-surfacing; fixes V1's 50/100 page caps), a **review-only import wizard** (`/import/google`: select → preview → import → done; **mirrors the full GC roster**, not a pick-list; loading/error/empty states), `POST /import-roster` (class upsert by `(school_id, google_course_id)`; teacher-of-record IDOR guard incl. a 23505 double-import race recovery; server-side roster re-fetch), and ONE shared **two-way reconcile engine** `reconcileCourseRoster` driven by THREE triggers — import, on-demand `POST /sync` (teacher OR same-school admin via `guardClassAccess`), and a **nightly Vercel cron** (`/api/cron/gc-roster-sync`; CRON_SECRET timing-safe dual-accept; per-teacher isolation; class-tenant scoping). Engine: match-by-google-id-then-email / `ensureAuthUser` create (account-takeover-safe) / enroll stamping `source='google'`; **soft un-enroll** (`is_active=false`, NEVER deletes the student or their history — re-enrollable) only GC-sourced seats IN THIS class absent from the roster; a **trustworthy-roster guard** so an empty/incomplete GC fetch can NEVER mass-un-enroll (`removeSkippedSuspectEmpty`); idempotent; advisory-lock removed (overlaps self-heal via convergence). Identity anchor = the Google `userId` in `external_identities` (matches V1's GC `supportsSisId:false`; the SIS-student-ID anchor is a separate V1 feature, deferred). Migration **0024** (additive: `external_identities.email`/`last_seen_at`, `enrollments.source` per-class provenance, `UNIQUE(school_id, google_course_id)`, email index) APPLIED + verified live (advisors all-WARN, 0 new). Process: ground → plan → **pre-code 5-lens review (folded before code)** → subagent-driven TDD (12 tasks, per-task review; dedicated opus reviews caught a P0 advisory-lock `.catch` crash the tests had MASKED, the IMP-6 IDOR, a provenance-clobber) → **5-lens whole-branch adversarial review (0 CRITICAL, 4 IMPORTANT folded)** → Playwright preview (surfaced + fixed a blank-wizard UX gap). Gates: tsc 0, **vitest 2458/2458**, build 0. **Decisions (Marvin):** mirror the full roster; two-way sync with soft-archive (un-enroll preserves the student + history); empty-class never auto-empties (safe for year-end rollover); admins can Sync, connect/import stay teacher-driven; Open-CORE pin deferred to Seg 3. Strings → `STRINGS-FOR-BARB.md §Google Classroom — Seg 2`. **Deferred minors:** cron connection-read observability; verify existing crons' auth header (Bearer vs `x-cron-secret`); `users.email` casing normalize; per-school `google_classroom_enabled` gate still unwired.

**DONE + MERGED TO PROD — Wave 0 hardening (`main`, 2026-06-24, LIVE).** From the verified V1→V2 reverse audit ([[v2-pilot-feedback-and-reprioritized-queue]]): **rate limiter wired** (`src/lib/rateLimit.ts`, Upstash-backed 10 AI calls/min/user, **FAILS OPEN**, graceful no-op until creds — now ON `/api/attempts/{transcribe,tts,homework-tutor}`, which retires the previously-deferred "voice rate-limiter" follow-up); viewport meta (tablets); husky **pre-push** gate (`npm test && npm run typecheck`); global reduced-motion CSS; `actionChipLabel` "reteach now" → "reinforce now" ([[v2-reteach-is-reinforce]]). **Upstash creds still to be set in Vercel** to turn enforcement on (limiter is safe-off until then).

**DONE + MERGED TO PROD — Item 1: IA correction + roster import (`main` merge `085453d`, 2026-06-24; NO migration; pushed to prod).** Matches V1's IA: lesson authoring moved INTO the Lesson Library (browse + "＋ Create" toggle hosting the Content Studio tabs; `/upload` retired → redirect); nav **"Upload" → "Import Roster"** (a tabbed `/import`: From-a-file + the now-surfaced Google wizard); a V1-parity **file roster importer** — pure xlsx parser (`src/lib/roster/`), two engines (full 5-sheet `importRoster` + lean `importStudentsToClass`, both via takeover-safe `ensureAuthUser`, enrollments `source='file'`), template builder, routes (`POST /api/admin/roster/import` preview+commit, `GET …/template`, `POST /api/teacher/roster/import`). **Decision (Marvin): the full 5-sheet import is TEACHER-runnable** (STAFF_ROLES; each non-platform caller PINNED to their own school — Opus-verified NO cross-tenant write; only platform_admin may target another). New dep: SheetJS `xlsx`. Built subagent-driven TDD (10 tasks) + per-task reviews (2 Opus security passes) + a **27-agent whole-branch review** that caught a CRITICAL the per-task reviews structurally couldn't (admin full-import unreachable in the teacher-only shell → resolved by the teacher-run decision); **all 21 findings resolved** (incl. a raw-DB-error leak + the template's example rows that parsed as real data → fake accounts). Gates: tsc 0 / vitest 2606 / build 0 / a11y 49. Strings → `STRINGS-FOR-BARB.md §Import Roster`. **⚠️ Marvin: UI visuals NOT yet approved — a frontend-design polish pass on the Import Roster + Lesson Library "Create" surfaces is OWED (propose-only → approve).** Deferred: lean pre-import preview (rationale logged); demo seed still "Demo Lesson"/"Test Class" (the realistic English-Lit-gr7 + Math-gr9 seed = **item 1b, NEXT**). See [[v2-pilot-feedback-and-reprioritized-queue]].

**DONE + MERGED TO PROD — A.1b + A.2 (`main` ← `feat/a2-teacher-workflow`, 2026-06-24; NO migration).** **A.1b:** nav/heading/topbar "Roster" → **"Class Roster"**; the concept-gaps rail is now **quiet** on the Class Roster (only shows when there ARE class-wide gaps — no "all clear" card); realistic demo seed — **English Literature gr7 (Dana Whitfield) + Math gr9 (Marcus Bell)**, the same 8 students in BOTH, subject-themed lessons/quizzes, the latent `'numeric'` quiz-type bug fixed (engineered signals preserved on the English-Lit class; live demo reseeded). **A.2:** the **"Reinforce Assignment"** button in the gradebook drill-in → backgrounded generation of an EASIER (band='reteach') assignment for that student (`POST /api/teacher/assignments/reinforce`, Next `after()`, **also notifies SPARK** for parity), student-visible via the no-status-filter loader; and **background quiz generation** — the teacher is freed instantly (lesson + quiz row created fast, questions fill in via `after()`), the Quiz Library shows **"Building…"** for a 0-question quiz, and an empty quiz CANNOT be published (server-side 409 guard + UI disable). Built subagent-driven TDD + per-task adversarial reviews (caught+fixed: FIX-1 student-visibility verified; FIX-2 publishable-empty-quiz). Gates: tsc 0 / vitest green / build 0 / a11y 49. **Upstash creds now in Vercel → the rate-limiter ENFORCES from this deploy.** The Darius-card de-clutter folds into the **deferred UI redesign** (Marvin put the whole-UI redesign ON HOLD — "functionality first"). Strings → `STRINGS-FOR-BARB.md §Reinforce Assignment`. See [[v2-pilot-feedback-and-reprioritized-queue]].

**DONE + MERGED TO PROD — THE MOAT: class-level Comprehension Level + learning-style + over-time → Insights (`main` merge `298e143`, 2026-06-25; migration 0025 APPLIED + verified live to NEW CORE; demo reseeded; deployed).** The differentiator ([[v2-moat-coach-over-the-shoulder]], [[v2-moat-class-comprehension-decisions]]). The teacher **Insights** page now leads with a comprehension coach sentence ("3 students need another pass on X"), shows **Comprehension by skill** (whole-class tally in the 3 teacher verbs — "Literary Analysis · 3 Reinforce · 3 On Track · 1 Enrich" — `<details>` expand → student names grouped by bucket → each links to that student's Skill Map; class-scoped via `quizzes→quiz_questions.skill_id` in JS **and** query so a 2-class student never cross-contaminates), an **over-time trend** (`GradeTrendSparkline`, soft direction sentence, week labels — **no raw %**), and a **"How your class learns"** reassurance line (confidence-gated — excludes the inferred `emerging`/low-conf, NEVER per-student, copy says **"differentiate," not "adapt"**). Every section quiet-when-empty. **De-risk:** the per-student CL rollup already existed (`loadStudentSignals.per_skill_cl`) — this is aggregation + history + surfacing, not a new model. New: `loadClassComprehension` + `loadClassLearningStyle` + pure `classComprehension` helpers + `comprehensionObservation` lead + 3 Insights components (SkillsToFocus REPLACED); **migration 0025 `skill_state_snapshots`** (weekly per-`(student,skill)` CL history, deny-by-default RLS) written by the existing weekly-snapshot cron + a demo backfill; `isoWeekMonday` extracted to an import-safe util. Divergence LEFT per-student (not added to the class summary, per spec). Built: ground (6-agent workflow) → spec (5 decisions, Marvin-locked) → `writing-plans` → **pre-code adversarial review (6 lenses; 4 Crit + 2 Imp folded in BEFORE code)** → 9-task subagent-driven TDD (per-task review) → **whole-branch adversarial review (6 lenses → verify → synth, READY-TO-MERGE, 0 surviving crit/imp, 3 minors fixed)** → Playwright preview (Marvin-approved). Gates: tsc 0, **vitest 2664/2664**, build 0 (a11y + tokens). Strings → `STRINGS-FOR-BARB.md §Insights — Class Comprehension`. **Deferred (logged):** the "How your class learns" line is **quiet on the demo** (no ≥3 confident distinct styles) — seed LS variety to showcase / Barb-gate the copy; trend cold-start wording (spec §3 literal vs quiet-when-empty) = a Barb/Marvin copy call (quiet is the shipped, defensible behavior); CL still does NOT feed content *generation* (separate epic).

**DONE + MERGED TO PROD — PRODUCTION OPS: append-only audit log + soft license gate (`main` merge `ea2ff9e`, 2026-06-25; migration 0026 APPLIED + verified live to NEW CORE; deployed).** Hardening before paying schools ([[v2-prodops-audit-license]]). **Audit log:** `audit_logs` table (migration 0026, append-only — service_role INSERT via admin client, platform_admin SELECT, NO update/delete policy, `school_id` stamped, no FKs so the trail survives deletions) + a best-effort `logAudit()` helper (`src/lib/audit/logAudit.ts`) that **NEVER throws/rolls back the audited action**. Wired into the 5 LOCKED actions: `grade.override` (old→new), `roster.sync` (teacher `/google/sync` = caller id, nightly cron = system/null), the GC `import-roster` first account-creation (`via:'import'`), `roster.import` (lean + full, real engine summary fields), `school.provision`, `spark.enable` (only on `ok===true`). NOT high-fives/alerts/quiz-publish. Platform-admin read route `GET /api/admin/audit` (no UI this epic). **Soft license gate:** widened `enforce_enrollment_limit` (migration 0026, one line `status IN ('active','trialing')`) so pilots can't enroll past `student_limit` — no-op for unlicensed schools, demo safe (8/300); over-limit imports surface a friendly seat-cap skip (23514), never a crash. **Trial-expiry stays OFF** (no pilot auto-suspends); expiry-gate / auto-suspend cron / provisioning-over-plan / audit UI all DEFERRED (would be dead code with no expiry mechanism — V1's exact trap). Built: ground (5-agent workflow incl. V1 reference) → spec (3 Marvin-locked decisions) → **pre-code review (1 CRITICAL: `assignments` has no `school_id` → resolve via `classes`; would've 404'd every grade override — + 4 Imp folded before code)** → 8-task subagent TDD (per-task review; C1-fix + I1/I2 sites got full reviews) → **whole-branch review (0 surviving crit/imp, 7 minors all fixed)**. Live-verified on NEW CORE: append-only RLS (0 authed write policies), trigger widened + still SECURITY DEFINER, audit row round-trips, advisors all-WARN (audit_logs not flagged). Gates: tsc 0, **vitest 2703/2703**, build 0. **Deferred-minor (logged):** spark-enable now calls `getUser` twice (guard already does — could optimize `guardPlatformAdmin` to return the caller later).

**DONE + MERGED TO PROD — GOOGLE CLASSROOM SEGMENT 3: publish to the stream + draft grade passback (`main` merge `fa144c8`, 2026-06-25; migration 0027 APPLIED + verified live to NEW CORE; deployed).** The "GC controls the flow" segment ([[v2-gc-seg3-publish-passback]], [[v2-google-classroom-epic]]). Teachers **publish** CORE quizzes & assignments into the Google Classroom stream as **DRAFT** `courseWork` (workType ASSIGNMENT) whose material links back into CORE (an **Open-CORE** `courseWorkMaterials` link pinned once per course; lands on normal login until Seg 4's silent SSO), and **send draft grades** for assignments back to Classroom — `updateMask=draftGrade` ONLY (no `assignedGrade`, **no `:return`** — the student never sees a CORE-pushed grade until the teacher releases it in GC). **Four-audience:** quizzes appear as a link but **NEVER push a grade** (diagnostic). **Key architecture (caught by pre-code review):** CORE `assignments` are a **per-student fan-out** (no class-wide `assignments.id`) — so publish + passback are **lesson-keyed** (the lesson IS the gradebook's assignment column); passback resolves `assignments WHERE class_id+lesson_id → homework_attempts(graded) → latest-per-student override-wins`. **Fail-soft** passback (per-student `[1s,3s]` retry, never aborts the batch, `not_posted_in_classroom` vs `skipped_not_linked` reasons, file-roster students w/o a Google link skipped). **No re-consent** — Seg-1 scopes already cover coursework/grade write. New: `src/lib/google/{publishToClassroom,gradePassback}.ts` + `classroom.ts` write helpers (gcWrite/createCourseWork/createCourseWorkMaterial/listStudentSubmissions/patchStudentSubmissionDraftGrade), `POST /api/teacher/google/{publish,grade-passback}`, publish UI on quiz+lesson library rows (gated on `google_course_id`, shows **"✓ In Google Classroom"** once published — Marvin feedback), Send-grades batch on the gradebook; migration **0027** `google_publications` (deny-by-default RLS + a partial-unique course_link index); `logAudit` `gc.publish`/`gc.grade_passback`. All GC actions say **"Google Classroom"** in full (disambiguate from CORE's own class-publish — Marvin). Built: ground → spec (3 decisions) → **pre-code review (NEEDS REWORK — caught the per-student-assignment→lesson re-keying + the DRAFT-no-submissions interaction, folded before code)** → 8-task subagent TDD → **whole-branch review (MERGE-WITH-FIXES — caught 4 real bugs: quiz `max_points` NOT NULL, publish cross-tenant IDOR, 2 reconnect-state UI dead-ends; all fixed)** → Playwright preview (Marvin-approved) → 2 UX follow-ups. Gates: tsc 0, **vitest 2789/2789**, build 0. Strings → `STRINGS-FOR-BARB.md §Google Classroom — Seg 3`. **Deferred:** Seg 4 silent-SSO launch (`GOOGLE_LAUNCH_STATE_SECRET` + proxy carve-out), Seg 5 Drive import, native GC quizzes, auto-push-on-grade, teacher-overridable maxPoints; the DRAFT-submissions spike (Task 0) is a post-merge manual check (engine handles the empty case via `not_posted_in_classroom`).

**DONE + MERGED TO PROD — GOOGLE CLASSROOM SEGMENT 4: student silent-SSO launch (`main` merge `b787f68`, 2026-06-25; NO migration; pushed → Vercel prod build triggered).** The segment that makes the Seg-3 Open-CORE link *work*: a student taps the public link in their Classroom stream (`${APP_BASE_URL}/?gc=<quiz|assignment>&id=<id>`) and lands authenticated in CORE **with no password** ([[v2-gc-seg4-silent-launch]]). Flow: the proxy diverts an unauthenticated `/?gc=` to a new **launch initiator** (`GET /api/auth/google/launch`) that sets a one-time `__Host-` nonce cookie + an **HMAC-signed `launch:` state** (10-min TTL, timing-safe) and redirects to Google with `prompt=none` + **identity scopes only** (`openid/email/profile` — students never grant classroom scopes); the existing `/api/auth/google/callback` **branches on the `launch:` state** (teacher-connect path byte-unchanged) → verifies state+nonce → `exchangeCodeForTokens`→`getGoogleProfile` → **rejects `verified_email:false`** → derives the candidate school from the launched resource → `resolveExternalIdentity` (Google userId, school-scoped) → **`role==='student'` + ACTIVE-enrollment-in-the-resource's-class** gate → mints a real Supabase session (`generateLink` magiclink → `verifyOtp`, mirroring `/auth/callback`) → deep-links (**assignment** = the student's OWN `assignments` row → `/student/assignments/<id>`; **quiz** = the EXACT quiz via `/student/quiz?quizId=<id>`, route re-gated published+enrollment — Marvin). No match → friendly public **`/launch/unmatched`**, **never auto-creates**. New: `src/lib/google/{launchState,launchResolve}.ts` + `buildLaunchAuthUrl`/`LAUNCH_SCOPES`, `GET /api/auth/google/launch`, the callback student branch, the proxy carve-out (initiator + `/launch/unmatched` public; callback let through ONLY on a `launch:` state — never fully public), root `page.tsx` authed-`?gc=` deep-link. **NO migration** (reuses Seg-1/2 `external_identities`/`google_connections`). Built: ground → spec (4 Marvin decisions) → **pre-code review (0 crit; folded `__Host-` nonce cookie, the active-enrollment gate, profile type, traversal guard; rate-limiter deferred)** → 8-task subagent TDD → **whole-branch review (0 crit/1 imp — fixed: the email-fallback resolver now binds to the authenticating Google id so a recycled email can't override a different link; the `__Host-` nonce is now cleared via an attribute-matched `set('',maxAge:0)` not a bare `delete()` which the browser rejects for a `__Host-` cookie)** → Playwright preview of `/launch/unmatched` (Marvin-approved). Gates: tsc 0, **vitest 2837/2837**, build 0. **⚠️ GO-LIVE PENDING — DORMANT (fail-closed to `/login`) until `GOOGLE_LAUNCH_STATE_SECRET` (32+ random bytes) is set in Vercel (`new-core`, Prod+Preview) + redeploy;** reuses the Seg-1 OAuth client + the already-registered callback URI (no GCP change). **Deferred:** per-IP rate limiter on the two public launch surfaces (classroom-NAT false-throttle risk); the authed-user-hits-initiator re-mint edge; **Seg 5 (Drive doc import)** is the last GC segment.

**REPRIORITIZED near-term (Marvin, 2026-06-24) — supersedes the locked sequence below for the immediate items:** Wave 0 ✅ → Item 1 IA correction ✅ → item 1b (Class Roster, quiet rail, realistic seed) ✅ → teacher-workflow fixes (Reinforce Assignment → easier work; background quiz gen) ✅ → the moat (class CL + Learning-Style + over-time; Insights→coaching) ✅ **DONE+LIVE (`298e143`, 2026-06-25)** → production ops (audit log + soft license gate) ✅ **DONE+LIVE (`ea2ff9e`, migration 0026, 2026-06-25)** → GC Seg 3 (publish + draft grade passback) ✅ **DONE+LIVE (`fa144c8`, migration 0027, 2026-06-25)** → GC Seg 4 (student silent-SSO launch) ✅ **DONE+MERGED (`b787f68`, 2026-06-25; NO migration; go-live pending the `GOOGLE_LAUNCH_STATE_SECRET` in Vercel)** → **NEXT: SPARK Challenges page fix (queued — dedupe/drill-in/condense-by-student/hover-tooltip) OR chapter-level eval OR GC Seg 5 (Drive import) (Marvin's pick)**. Then **decide B/C/D/E** (Profile, Support tix, student/parent/school-admin/super-admin pages — note school-admin has NO shell yet). **DEFERRED:** the **whole-UI redesign** (ON HOLD, Marvin — Margin Note prototype on `feat/look-and-feel-teacher-home` awaits) incl. the Darius-card de-clutter + owed Import-Roster polish. Flag-to-Barb: student-% dignity bands (her lock) + parent forbidden-words guard (ships with Epic 4).

**NEXT — program sequence LOCKED (Marvin, 2026-06-23), per [[v2-authoring-platform-program]]:**
1. **Content Studio: ✅ COMPLETE. GC Seg 1 (Connect + token vault): ✅ LIVE. GC Seg 2 (Roster import + two-way sync + nightly cron): ✅ LIVE (migration 0024). GC Seg 3 (publish + draft grade passback): ✅ DONE + MERGED + migration 0027 APPLIED live (`fa144c8`, 2026-06-25). GC Seg 4 (student silent-SSO launch): ✅ DONE + MERGED (`b787f68`, 2026-06-25; NO migration; go-live pending `GOOGLE_LAUNCH_STATE_SECRET` in Vercel — dormant/fail-closed until set).** → **NEXT: GC Seg 5 (Drive doc import) — the last GC segment.** Plus the deferred **voice rate-limiter** on `/api/attempts/{transcribe,tts}` + the GC Seg 4 per-IP launch rate-limiter. (0023 = `behavioral_signals_rls`; Seg 2 took 0024, Seg 3 took 0027 — see [[v2-db-incident-0013-0014-revert]].)
2. **Then the rest, reordered (Marvin): Google Classroom → Profile Settings → Support Ticket.** GC = OAuth + roster/assignment/grade sync (heaviest; reference V1 + the SPARK integration pattern). Profile = avatar upload (`avatars` bucket) + name + password; **email read-only/admin-only** (matches V1; Google-sourced identity). Support = `support_tickets`/messages tables + Help entry + platform-admin inbox.

Per epic/segment: ground in V1 → (epic spec exists at `docs/superpowers/specs/2026-06-23-content-studio-design.md`) → `writing-plans` → subagent-driven-development + adversarial review → Playwright preview → Marvin merge call. Optional gradebook follow-up: the pre-existing topbar-greeting hydration (#418) fix.

Per epic: ground in V1 → spec (user sign-off) → `writing-plans` → subagent-driven-development + adversarial review before merge. V1 (`C:/users/inteliflow/core`) is the completeness floor; V2 is the upgrade + future base. Self-serve trial onboarding (`docs/superpowers/specs/2026-06-19-trial-onboarding-design.md`) written. BR/EduFlux/pt-BR deferred.

## Deployment

Targets Vercel (Next.js zero-config). **Vercel CLI is now installed** (54.14.2; team `inteliflow` / Inteliflow-projects). **Live production:** V2 = project `new-core` → **newcore.inteliflowai.com**; SPARK = `spark-platform` → spark.inteliflowai.com; V1 = `core-platform` → app.inteliflowai.com. Env vars live in Vercel; pushes to `main`/`master` auto-trigger Production builds. Note: repo's `.vercel/project.json` stale-links to a duplicate `core-v2` project (cleanup pending).
